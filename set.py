marks = {92,56,67,89,90,90,90}

# we can declare an empty set by "marks = set()"
# set is unordered , so we can't access any element by its index
# only we can iterate to get access

for mark in marks:
    print(mark)

# set removes all the duplicate values