marks = (92,56,67,89,90,90,90) # declare the tuple

# we can declare an empty tuple bu 'marks = ()'
# no update is allowed , only we can see the index and we can count how many times each element appear

print(marks.count(90)) # count how many times 90 appear in the list

print(marks.index(90)) # will print only the first index